{
	"id": "code-bugfix",
	"family": "code",
	"timeoutMs": 420000,
	"prompt": "Running `node packages/nana-stage/tests/blocks.test.mjs` in this repository currently fails one assertion. A single line of source code is wrong. Find it, fix it so the whole suite passes, and change nothing except that one source file. Do not edit anything under `packages/nana-stage/tests/`. When you are done, reply with ONLY the path of the file you changed.",
	"mutations": [
		{
			"file": "packages/nana-stage/lib/blocks.mjs",
			"find": "parseFloat(n.toFixed(2))",
			"replace": "parseFloat(n.toFixed(1))",
			"note": "fmtNum rounds to 1 decimal instead of 2 — breaks exactly one assertion ('chart text: a per-series summary, not the points'), 111 others still pass"
		}
	],
	"keyDerivation": {
		"command": "apply the mutation, then: node packages/nana-stage/tests/blocks.test.mjs",
		"output": "mutated: exit 1, 111 PASS + 1 FAIL. Pristine: exit 0, 112 PASS, 0 FAIL, every PASS printed by the test file itself. `passLines: 112` is that trusted count."
	},
	"check": {
		"type": "all",
		"checks": [
			{
				"type": "changed-paths",
				"allow": [
					"packages/nana-stage/lib/blocks.mjs"
				],
				"protect": [
					"packages/nana-stage/tests/**"
				],
				"withinLines": {
					"packages/nana-stage/lib/blocks.mjs": [
						150,
						153
					]
				},
				"denyAdded": true,
				"note": "Layers 1 and 2 of the anti-forgery rule: the change must fall inside fmtNum's declared line range, and no ADDED line may introduce process.exit/console./require( etc. Those tokens do not fix rounding; they manufacture evidence."
			},
			{
				"type": "file",
				"path": "packages/nana-stage/lib/blocks.mjs",
				"notContains": "parseFloat(n.toFixed(1))",
				"note": "the DECLARED defect must actually be gone. Only its absence is required, so a different correct rewrite of fmtNum is still allowed."
			},
			{
				"type": "trusted-suite",
				"argv": [
					"packages/nana-stage/tests/blocks.test.mjs"
				],
				"trust": [
					"packages/nana-stage/tests/blocks.test.mjs"
				],
				"passLines": 112,
				"note": "Layer 3: the suite runs under a sentinel copied in from OUTSIDE the fixture. process.exit throws, and PASS lines are counted only when the TEST FILE printed them — 112 PASS lines fabricated by blocks.mjs are forgery, not a pass."
			}
		]
	},
	"declaredSite": {
		"file": "packages/nana-stage/lib/blocks.mjs",
		"function": "fmtNum",
		"lines": [
			150,
			153
		],
		"note": "fmtNum spans lines 150-153 of the pinned fixture; the mutation is on 152. The fix must live inside that function."
	}
}
