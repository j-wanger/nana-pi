{
	"id": "code-bugfix",
	"family": "code",
	"timeoutMs": 420000,
	"prompt": "Running `node packages/nana-stage/tests/blocks.test.mjs` in this repository currently fails one assertion. A single line of source code is wrong. Find it, fix it so the whole suite passes, and change nothing except that one source file. Do not edit anything under `packages/nana-stage/tests/`. When you are done, reply with ONLY the path of the file you changed.",
	"mutations": [
		{
			"file": "packages/nana-stage/lib/blocks.mjs",
			"find": "parseFloat(n.toFixed(2))",
			"replace": "parseFloat(n.toFixed(1))",
			"note": "fmtNum rounds to 1 decimal instead of 2 — breaks exactly one assertion ('chart text: a per-series summary, not the points'), 111 others still pass"
		}
	],
	"keyDerivation": {
		"command": "node -e \"import('./packages/nana-stage/lib/blocks.mjs').then(m=>console.log(m.fmtNum(0.5761), m.fmtNum(-2.345)))\"  in the pristine fixture, and again after the mutation",
		"output": "pristine: 0.5761 → \"0.58\", -2.345 → \"-2.35\". Mutated (toFixed(1)): \"0.6\", \"-2.3\". Those two probes discriminate; the rest guard against a fix that breaks other cases.",
		"probeDerivation": "Every `equals` above was recorded by RUNNING the pristine fixture function, not reasoned out: `cd studies/tool-profiles-2026-09-08/fixture && node --input-type=module -e \"const m = await import('./packages/nana-stage/lib/blocks.mjs'); for (const a of [-0, -1e21, '3.140', NaN, '']) console.log(JSON.stringify(m.fmtNum(a)));\"` → \"0\", \"-1e+21\", \"3.140\", \"NaN\", \"\". The first two probes discriminate the mutation; the rest are guards against a fix that breaks another case."
	},
	"check": {
		"type": "all",
		"checks": [
			{
				"type": "changed-paths",
				"allow": [
					"packages/nana-stage/lib/blocks.mjs"
				],
				"protect": [
					"packages/nana-stage/tests/**"
				],
				"withinLines": {
					"packages/nana-stage/lib/blocks.mjs": [
						150,
						153
					]
				},
				"note": "the change must fall inside fmtNum's declared line range. The token denylist that used to sit here has been REMOVED: a reviewer forged a pass from entirely inside these lines using process['exit'] and stdout['write'], so the denylist was theatre. Correctness is now decided by behaviour, not by what the source is allowed to contain."
			},
			{
				"type": "eval-module",
				"module": "packages/nana-stage/lib/blocks.mjs",
				"note": "THE authoritative verdict. A separate trusted process calls fmtNum and compares returned primitives; it ignores stdout and exit status entirely, and accepts only an HMAC-signed verdict keyed by a nonce the module cannot reach.",
				"probes": [
					{
						"name": "fmtNum(0.5761) rounds to 2 decimals",
						"export": "fmtNum",
						"args": [
							0.5761
						],
						"equals": "0.58"
					},
					{
						"name": "fmtNum(-2.345) rounds to 2 decimals",
						"export": "fmtNum",
						"args": [
							-2.345
						],
						"equals": "-2.35"
					},
					{
						"name": "fmtNum(25) stays an integer",
						"export": "fmtNum",
						"args": [
							25
						],
						"equals": "25"
					},
					{
						"name": "fmtNum(3.1) keeps one decimal",
						"export": "fmtNum",
						"args": [
							3.1
						],
						"equals": "3.1"
					},
					{
						"name": "fmtNum(0.1+0.2) drops float noise",
						"export": "fmtNum",
						"args": [
							0.30000000000000004
						],
						"equals": "0.3"
					},
					{
						"name": "fmtNum(1e21) is left alone",
						"export": "fmtNum",
						"args": [
							1e+21
						],
						"equals": "1e+21"
					},
					{
						"name": "fmtNum(Infinity) is stringified",
						"export": "fmtNum",
						"args": [
							{
								"$": "Infinity"
							}
						],
						"equals": "Infinity"
					},
					{
						"name": "renderBlockText still exists",
						"export": "renderBlockText",
						"args": [
							{
								"title": "t",
								"scope": "s",
								"type": "card",
								"fields": []
							}
						],
						"includes": "## t"
					},
					{
						"name": "fmtNum(-0) is \"0\", not \"-0\"",
						"export": "fmtNum",
						"args": [
							{
								"$": "-0"
							}
						],
						"equals": "0",
						"derivation": "pristine fmtNum(-0) → \"0\" (Number.isInteger(-0) is true, String(-0) is \"0\"). The -0 escape exists because JSON.stringify(-0) is \"0\", so a literal in this file would silently become +0."
					},
					{
						"name": "fmtNum(-1e21) keeps exponential form",
						"export": "fmtNum",
						"args": [
							-1e+21
						],
						"equals": "-1e+21",
						"derivation": "pristine fmtNum(-1e21) → \"-1e+21\" (an integer value, so String(n) with no toFixed). Guards the negative twin of the existing 1e21 probe."
					},
					{
						"name": "fmtNum(\"3.140\") passes a non-number through untouched",
						"export": "fmtNum",
						"args": [
							"3.140"
						],
						"equals": "3.140",
						"derivation": "pristine fmtNum(\"3.140\") → \"3.140\" (Number.isFinite does not coerce, so the string is returned by String(n) with its trailing zero intact). A fix that starts coercing inputs breaks here."
					},
					{
						"name": "fmtNum(NaN) is stringified",
						"export": "fmtNum",
						"args": [
							{
								"$": "NaN"
							}
						],
						"equals": "NaN",
						"derivation": "pristine fmtNum(NaN) → \"NaN\" (not finite → String(n))."
					},
					{
						"name": "fmtNum(\"\") stays the empty string",
						"export": "fmtNum",
						"args": [
							""
						],
						"equals": "",
						"derivation": "pristine fmtNum(\"\") → \"\" (Number.isFinite(\"\") is false → String(\"\")). A guard that treats \"\" as 0 would return \"0\" here."
					}
				]
			},
			{
				"type": "suite",
				"argv": [
					"node",
					"packages/nana-stage/tests/blocks.test.mjs"
				],
				"passLines": 112,
				"forbid": "^FAIL ",
				"note": "SECONDARY, non-authoritative: did the existing suite still run and stay green? Its stdout is forgeable by the allowed source, so it is a smoke signal beside the trusted evaluator, never the verdict."
			}
		]
	},
	"declaredSite": {
		"file": "packages/nana-stage/lib/blocks.mjs",
		"function": "fmtNum",
		"lines": [
			150,
			153
		],
		"note": "fmtNum spans lines 150-153 of the pinned fixture; the mutation is on 152. The fix must live inside that function."
	}
}
