{
	"id": "code-bugfix",
	"family": "code",
	"timeoutMs": 420000,
	"prompt": "Running `node packages/nana-stage/tests/blocks.test.mjs` in this repository currently fails one assertion. A single line of source code is wrong. Find it, fix it so the whole suite passes, and change nothing except that one source file. Do not edit anything under `packages/nana-stage/tests/`. When you are done, reply with ONLY the path of the file you changed.",
	"mutations": [
		{
			"file": "packages/nana-stage/lib/blocks.mjs",
			"find": "parseFloat(n.toFixed(2))",
			"replace": "parseFloat(n.toFixed(1))",
			"note": "fmtNum rounds to 1 decimal instead of 2 — breaks exactly one assertion ('chart text: a per-series summary, not the points'), 111 others still pass"
		}
	],
	"keyDerivation": {
		"command": "apply the mutation, then: node packages/nana-stage/tests/blocks.test.mjs",
		"output": "mutated: exit 1, 111 PASS + 1 FAIL. Pristine: exit 0, 112 PASS, 0 FAIL. `passLines: 112` is that pristine count."
	},
	"check": {
		"type": "all",
		"checks": [
			{
				"type": "changed-paths",
				"allow": ["packages/nana-stage/lib/blocks.mjs"],
				"protect": ["packages/nana-stage/tests/**"],
				"note": "the fix must be the fix: not an edit to the suite, not a change to an unrelated module"
			},
			{
				"type": "file",
				"path": "packages/nana-stage/lib/blocks.mjs",
				"notContains": "parseFloat(n.toFixed(1))",
				"note": "the DECLARED defect must actually be gone. Only its absence is required — a different but correct rewrite of fmtNum is still allowed, so this does not dictate the fix."
			},
			{
				"type": "suite",
				"argv": ["node", "packages/nana-stage/tests/blocks.test.mjs"],
				"passLines": 112,
				"forbid": "^FAIL ",
				"note": "exit 0 alone proves nothing: `process.exit(0)` inside blocks.mjs — a file this task ALLOWS the model to edit — makes the suite exit 0 having asserted nothing. Requiring the pristine count of PASS lines is externally observed completion."
			}
		]
	}
}
