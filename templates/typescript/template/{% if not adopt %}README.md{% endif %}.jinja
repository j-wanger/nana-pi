# {{ project_name }}

{{ description }}

## Setup

```bash
pnpm install
pnpm check   # typecheck + lint + test
```

Scaffolded from the nana-pi TypeScript template; `uvx copier update` re-syncs it
when the template evolves. Agent-facing structure and rules live in `AGENTS.md`.
