# Adjudication — round 7 (pi gpt-5.6-sol, VERDICT: BLOCK)

Boundary fold: PASS. One finding, REAL; the reviewer could construct no other.

- **B/C. `passableFromTop` contradicts enterability — REAL. Fold by SUBTRACTION.** The property
  was ratified (round-0 discussion) under the assumption that top view snaps you onto surfaces.
  v2 chose "frozen depth is the truth" (B1), after which the property has no coherent job: in top
  view a player above a pool is simply in a `none` cell at surface + 1, which is enterable by
  §3.3; whether the pool "holds" them is a support question, and §3.7 already says liquid is not
  support (no gravity in top view; sink or float per density once gravity returns). v8 removes
  `passableFromTop` from the v1 table (three properties remain: state, hardness, density) and
  deletes the §3.4 sentence that referenced it. **Jake may veto** — it was his addition; if a
  walk-on-water mechanic is wanted, it returns as a material `state` (e.g. `ice`) rather than a
  flag that fights the enterability rule.

## Verdict on the verdict

Correct. v8 removes a rule rather than adding one; round 8 checks it.
