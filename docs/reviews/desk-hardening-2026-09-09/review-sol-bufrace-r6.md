1. **Abandoned-id failure toast — PASS.** The bounded map retains the command and emits `bash: <command> — <error>`.

2. **Declared 8-run bound — FINDING.** README rule five and the punch-list incorrectly say the **ninth/newest** run’s terminal event triggers no read. The implementation evicts the **oldest** of nine; the Known limit states this correctly.

VERDICT: BLOCK
