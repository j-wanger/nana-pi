# Session archive — 2026-09 (nana-pi)

Entries before 2026-09-18 were written in nana-agent-loop's archive and copied here verbatim.
From 2026-09-18 this repo's narrative is written here (newest first).

## 2026-09-18 (3) — a blank folder becomes a nana project in one command: `nana-setup project`, seeds shared by the copier template and adopt-structure; three sol rounds, landed at the cap; v0.5.0 tagged

Jake: "how does it work in a new blank folder?" Verified: after `nana-setup install`, a session in a blank folder already gets the shared rules, a self-healed project memory dir, the umbrella objective and every pi extension — but nothing wrote OBJECTIVE.md, HANDOFF.md or `docs/sessions/`, the three files that make the objective hook and the close protocol fire for THAT project, and the scaffold/adopt skills existed only in pi. Built: one seed source in `templates/_shared/` consumed by the copier template (jinja include wrappers, `_skip_if_exists` for adopt mode), by the adopt-structure skill (new step 6; the fallback render now takes the name from an env var, never command text), and by `nana-setup project [dir] [--name] [--dry-run] [--check]` from any shell (git init unless inside a repo; seeds; stub AGENTS.md with the canonical section + CLAUDE.md alias; `.pi/nana-pack.json` on-ramp; knowledge refresh under a 60 s deadline). Never overwrites: lstat-based existence, symlinks and directories skipped by name. Review corpus + adjudication: `docs/reviews/nana-project-2026-09-18/` (r1 HIGH write-through, r2 HIGH shell interpolation in the skill text, r3 HIGH ✗-but-exit-0 — all implemented). Smoke on main: a hostile `--name` (`$(x) $&`) lands byte-literally, no canary, check ✓.

**Spend:** 1 Opus worker (~260k tokens incl. three folds), 3 sol rounds, 0 API credits. **NEXT:** fresh-machine feel test (clone → `pi install` → `nana-setup install` → `nana-setup project` in a new folder → open a session). Tagged v0.5.0 so `scaffold-*` and `pi install git:` users get the seeds.

## 2026-09-18 (2) — nana-setup: the whole experience now ships from this repo (Claude Code half + user-scope pi config + PATH + desk service); three sol rounds under the cap, r3 HIGH closed by subtraction

Jake: "is the whole experience packaged into the nana-pi install?" — no; `pi install` shipped the pi half only. Hooks, rules, settings wiring, two-tier memory, pi user config, `pi-review` on PATH and the desk launchd service were hand-maintained dotfiles on one Mac. Built `packages/nana-setup` (`install` / `doctor`, zero deps): hooks and the soul rule are SYMLINKED from the repo so `git pull` updates them; settings.json merged under an O_EXCL lock with an atomic rename and a post-write recheck; the shared-memory hook self-heals each project's `shared` link at session start by reproducing Claude Code's project-key rule (hash for keys over 200 chars, non-ASCII skipped with a notice); pi config and objective seeded only when absent; `pi install` skipped when the repo is already registered (identity by git common dir, so worktrees count); `--desk` renders the plist. `nana-personal.md` stays private — the repo ships an example. Review corpus + adjudication: `docs/reviews/nana-setup-2026-09-18/`. Run for real from `~/nana-pi`: 4 changed (the four hand files became symlinks, backed up), 13 already in place; doctor all ✓; second run "nothing to do".

**Spend:** 1 Opus worker (~325k tokens incl. three folds), 3 sol rounds, 0 API credits. **NEXT:** fresh-machine feel test = clone, `pi install`, `nana-setup install`, `doctor`, start a session in a product repo.

## 2026-09-16 (3) — pi-side knowledge pull LANDED (nana-pi `215fdef`); the "needs a tool" premise was wrong

Jake opened with the handoff's own claim: pi has no prompt-keyed pull because its agent-start event carries no prompt text, so it would need a tool. Verified against the installed pi 0.84.4 types before designing anything: `before_agent_start` carries `event.prompt` (raw text after skill/template expansion), and `input` carries `text`. The claim had been written into HANDOFF and the session log last session without a source line. No tool; a thin extension.

**Design (subtraction first):** ONE producer of pointers. `packages/nana-knowledge/extensions/nana-knowledge.ts` spawns the existing `hook` CLI out-of-process on `before_agent_start` with `{prompt (8 KiB slice), session_id, cwd, source:"pi"}` and injects what it prints as a persistent custom message (`display: true`; the desk already renders custom messages as a `⧉` bubble, so nothing UI-side was built). Out-of-process because pi has no per-handler timeout and node:sqlite must not load in pi's process; `message` not `systemPrompt` because pi rebuilds the system prompt every turn and the per-session dedup would make a system-prompt pointer vanish; no config key. Installed by adding the package path to `~/.pi/agent/settings.json` (backup `settings.json.bak-20260916`); the root manifest also lists it for `pi install git:`.

**Build:** one Opus worker under a brief (build + fold), seat verified every claim (17 test files, 0 fail, real exit codes; three `pi -e … -p` smoke runs each leaving one `source:"pi"` line in `pull.log` and one `customType:"nana-knowledge"` message in the session file). **Review (`~/nana-pi/docs/reviews/pi-knowledge-pull-2026-09-16/`):** sol r1 BLOCK — execFile's `timeout` is a kill attempt, its callback fires on close, so a SIGTERM-trapping child hung the turn (worker's negative control: STILL-HANGING at 3 s on the r1 code). Fold: parent-side timer resolves null first, then SIGKILL; child runs on `node` even under a bun-run pi; the stray empty-prompt guard removed; README states the accumulation trade-off (one ≤ 2000-char user-role message per fresh pull, compaction may summarize it). r2 LAND with a MEDIUM: with SIGKILL set, the SIGTERM-deaf test no longer proved the parent timer; sol proposed a child whose grandchild holds stdout. Seat measured before adopting it: **that child settles in 207 ms with the timer deleted** — Node's execFile destroys the child's stdio before signalling. The only child a kill cannot end is one wedged in an uninterruptible syscall, which no real process can simulate, so the honest test fakes execFile (an `execFileFn` seam, mirroring `hook.ts`'s `spawnFn`); negative control STILL-HANGING without the timer, 203 ms with it. r3 LAND. Residuals carried in the README: overlap-dedup race repeats a pointer; compaction; double registration if root AND package are both installed; bun path untested.

**Catches:** a reviewer's suggested fix can encode a false model of the platform — measure the fix's premise the same way as the finding's (the grandchild scenario would have shipped a test that proves nothing). The `[nana:knowledge]` pointer surfaced at THIS session's first prompt was the audit that named the gap — a first observed pull-to-use. The dual-review choreography was hand-run again (3 rounds) → FRICTIONS 08-21 entry recurrence.

**Objective score:** CLEAR — priority line (consistent: the same pull in both runtimes, one producer, one log). **Asset test:** the knowledge index now has two readers; `pull.log` distinguishes them. **Spend:** 1 Opus worker (~300k tokens, two rounds), 3 sol rounds, 3 sol smoke prompts, 0 API credits. **NEXT:** feel the pointers in nana code for a week (they now appear as a `⧉ nana-knowledge` bubble); then the pull.log → citation checker; raw-only wikis in or out is still Jake's call.

## 2026-09-16 (2) — objective + priority made live, review round cap, knowledge pull: the "more autonomous, not better" diagnosis measured, then fixed at the read side

Jake, thinking out loud: three months of rising autonomy without more consistent results; agents "choose the incorrect priorities and overspend"; sessions forget what was agreed; research is stored but not used; wants the map live for agents. Seat's read before any build: the objective is unranked and never enforced at spend time; the governed-loop contract (doneWhen + cost cap) was dropped when the runner went dormant while autonomy stayed; forgetfulness is a READ-side problem (seven deposit homes, one read moment). Jake: "lets try" + two rulings — objective PLUS current priority, and if a session cannot answer to either we discuss updating them even when the steering was his; plus a knowledge-wiki-style repository with runtime pull.

**Measure first (two Opus audits, `docs/audits/`).** Session spend vs objective, all 39 September entries + 12 sampled July/August: wrong priorities do NOT originate in agent self-redirect (1 of 39, caught same session) — 20 of 39 open with Jake naming a new direction, and the agent then never ranks, costs, or closes lanes; objective answerability collapsed from 23/24 (Jul–Aug) to 4/39 (Sep) because NORTHSTAR (07-01) finished its build program and nothing replaced it; the dominant overspend shape is review rounds made free by the flat Codex subscription (100+ rounds in September; the logs record implementing and instruments finding what rounds missed); second shape = build before direction settles (a day's work discarded the next day, ×3). Knowledge utilization: 72 grounded research articles with ZERO reads outside `research/`; ten wikis unqueried since June, the surfacing hook never wired here or in nana-pi; DOCTRINE the only ledger with read-back (84% of entries cited ≤ 1); research that WAS cited was written for a live decision — citation tracks "open decision when written", not storage quality.

**Jake's objective line (confirmed, modified):** build products with agents, with nana-pi as the shared toolkit AND experience. **Priority:** make the nana-pi experience consistent, coherent and effective.

**Built (seat + three Opus workers, sol reviews):**
- `OBJECTIVE.md` — single source; global Claude Code SessionStart hook prints the two lines in every repo (`~/.claude/hooks/nana-objective.sh`, settings backed up); pi gets it through the new nana-pack `nana-objective` extension (user-scope path only — a project config cannot inject; all five session reasons; 4000-char cap; an `OBJECTIVE UNAVAILABLE: <cause>` marker instead of silence). HANDOFF now points at it and the close protocol scores CLEAR/STRETCH/CANNOT; CANNOT → decision marker (slug prefix `objective-`). A new lane opens only by editing the priority line.
- **Review round cap = 3 per item** — `app/scripts/review-round.mjs` (pure) + `pi-review.mjs` refuses r4+ without `--over-cap "<what changed>"`; wrapper options parsed only before `--`; tests on the real corpus names (sol r1 caught that `round-N` was missed).
- **`packages/nana-knowledge`** (nana-pi) — FTS5 on Node's bundled SQLite (verified unflagged), BM25 over `~/.pi/agent/nana-knowledge/sources.json` roots; `hook` subcommand on the global UserPromptSubmit hook: skips short/slash/harness-notification prompts, top-3 pointers ≤ 2000 chars framed as untrusted DATA, per-session dedup, detached incremental rebuild under an atomic `wx` lock when > 1 h old, hard 1500 ms exit timer armed before stdin, stdin 256 KB cap; `pull.log` JSONL = the utilization instrument (sol wanted it removed as monitoring; rejected — it is the one thing that answers Jake's doubt). First build: 13,382 files / 273 MB; after dropping `raw/` scrape dirs and `reviews/` corpora (they dominated 2 of 3 real queries): 499 files / 603 rows / 8 MB / 0.3 s. Live check on this session's own prompt returned the three right docs.
- Review ladder (`~/nana-pi/docs/reviews/objective-knowledge-2026-09-16/`): sol r1 BLOCK — budget observational not enforced, lock TOCTOU, silent-missing objective, `round-N` regex miss, injection framing, relative objective path vs cwd, tests mirroring implementation; folded (one Opus worker + seat) → sol r2 BLOCK — stale-lock reclaim was remove-then-create (TOCTOU), configured-but-missing roots were PURGED from the index, regex accepted a trailing letter; folded (the fold worker measured my rename-aside prescription: 32 procs × 25 races, remove-then-create 3 double-winners, rename-aside 12, reclaim-under-a-second-lock 0 → landed the measured one, not mine) → sol r3 BLOCK on one item: a lock owner that is ALIVE could still be reclaimed after 10 min → seat fix (alive owner never stale; TTL only when no pid can be read) + tests, landed without a fourth round per the cap. Residuals recorded: a test heading still says "wall-clock bound"; changing a root's `kind` is not re-parsed until content changes.

**Catches / frictions:** the commit gate fired on a worker's commit in ~/nana-pi (it gates on THIS repo's index) → FRICTIONS; the knowledge hook fired on a harness task-notification and pulled review-brief noise → skip rule + `reviews/` excluded; two wikis (agent-memory, agentic-engineering) turned out to be raw-only and are now out of the index — a curation gap, Jake's call.

**Objective score for this session:** CLEAR — priority line (nana-pi experience: consistent = same objective everywhere; coherent = one knowledge surface; effective = round cap). **Asset test:** OBJECTIVE.md (read every session start, two runtimes), the knowledge index (read every prompt), pull.log (read by the future checker). **Spend:** 5 Opus workers (~700k tokens), 2 sol rounds, 0 API credits. **NEXT:** feel the objective line + pointers for a week; then the pull.log → citation checker (measure before more retrieval machinery); pi-side pull (needs a tool, `before_agent_start` has no prompt); map annotations as the same read-side pattern, only after the above prove felt-useful.

## 2026-09-16 — nana code: Jake's five-item UX batch LANDED (`fb2175a`), four sol rounds, a latent test race found by bisect

Jake: clean background processes, back to nana-pi, five feedback items — files changed + line diff after a turn with an inspectable floating diff; skills added mid-session not found; `/skill:` triggers dumping the whole SKILL.md; no visible sign a streaming turn is alive; reasoning streamed the Claude-Code way (collapsible card). Seat restarted the launchd desk (4 days on the stale `server.mjs`, zero live children), left `~/my-chan`'s uvicorn alone.

**Design, before any build (verified against pi 0.84.4 source, not memory):** RPC has NO reload command; an extension can `ctx.reload()` (re-reads settings.json + rediscovers) → nana-pack gains `/reload-runtime` (pi's own `/reload` is a TUI built-in that shadows an extension command of that name), the desk's `/reload` sends it as a prompt and auto-detects gained skill paths via `/api/resources` (3 ms warm) on focus / before send / settings change. `/skill:name` is expanded by pi into `<skill name=..>…</skill>` in the recorded user text → deterministic collapse. Files-changed = git working tree vs HEAD incl. untracked, session-bound read-only endpoints (NOT tool-call attribution: edit diffs are per-hunk, `write` has none) — the one assumption flagged to Jake.

**Build:** three Opus-4.8 lanes in worktrees (live-feel / changes / reload), all green alone; merge conflicts additive. Merged suite: `session-races.e2e` failed 1–3 checks per run. Bisect (5 throwaway worktrees: no reconnect refresh / no settled refresh / refresh no-op / cheap GET instead / refresh delayed 1.5 s) isolated it to the `/changes` GET on the reconnect hello; a fetch-order dump showed the held bash POST answering 1 ms before the reconnect's `get_messages` — the TEST released the POST on a wait (`bash-card` count / any rpc response) the pre-reconnect pane already satisfied. Fixed in the test: mark the first bubble, cut the stream, wait for an unmarked bubble (46/46 ×3, sol confirmed it cannot be satisfied without the read). Doctrine-shaped lesson: a wait condition that pre-state satisfies is a latent race; wait for the positive fact of the rebuild.

**Review ladder (`~/nana-pi/docs/reviews/desk-ux-2026-09-16/`):** sol r1 BLOCK — ~1 GiB of SYNC untracked reads per refresh; prompt posts while `/reload-runtime` still runs (5 s detach → `pending` read as done). r2 BLOCK — budget vs stale lstat size; poll-on-`get_commands` completion is heuristic (false completion / 15 s hold). r3 BLOCK — `limit+1` probe byte + unreserved concurrent reads; `desk_prompt_settled` can beat the waiter on a healthy connection. r4 LAND — MED on the dropped-SSE e2e proving less than claimed, folded after. Survivors: reserve-before-read (`plan(size)` synchronous between fstat and first read; reads stop AT the bound; O_NOFOLLOW+fstat on darwin/linux, win32 keeps lstat-first), server `settlePrompt()` ring of 32 replayed in every `desk_hello`, client `settledSeen` consulted before installing a waiter, `promptId` in every prompt response. Removed under review: `awaitPendingReload()` polling + ceiling; a vacuous stale-tick test. Landed `fb2175a` (32 commits over `d6c53e0`; 19 files, +4.2k), desk restarted, feel check is Jake's.

**Spend:** 3 lane Opus workers (~720k) + 3 fold Opus workers (~900k), 4 sol rounds, 0 API credits. **Open for Jake:** the changes bar's baseline (git/HEAD vs conversation-attributed); whether the auto-reload's focus/send triggers are enough in practice (no polling timer by design); Windows untested for O_NOFOLLOW fallback and the changes endpoints.

## 2026-09-09 (2) — nana-pi desk: the three astra mediums closed (buffers, races, stage key) — 16 review rounds, three subtractions

Jake: "All nana pi gaps closed?" → no; then "Ok lets close these three" (unbounded server buffers · client-side races · per-spawn stage signing key, design B). Stakes read: medium blast, and the stage key is a provenance boundary, so it got adversarial review before landing. Shape: three Opus workers, one per item, each in its own git worktree off `034be76`; the seat wrote briefs (confirm-before-fix, failure-first through the real chokepoint, declare every observable change), merged into an integration branch, ran the suite sequentially, and folded every review round; gpt-6-astra on the stage key, gpt-5.6-sol on buffers + races. Baseline captured first: 30 files green; 32 green at every merge checkpoint; final on main `d6c53e0` (46 commits, 48 files).

Buffers landed at sol r2 with four caps (stdout line 64 MiB sized against a measured 18.4 MiB `get_messages` line; SSE 8 MiB/client drop-and-resync; 64 in-flight RPCs/child incl. the `/bash` fill site the brief had not named; app `data` stdout 8 MiB with the shared tree-kill). Races took 8 rounds and the stage key 8, and the pattern in both was the same: each lane grew a mechanism under review pressure that then owned the next blocker, and the round that closed it was a REMOVAL. Races: the count-based bash-card adoption (a compaction, a branch rewrite, another client, or a stale-generation run each add one same-command card, so "delta = 1" proves nothing) → removed; history wins, a terminal event after a rebuild triggers one read, a desk-side failure becomes a toast. Stage key: the single-file store with an advisory lock (its retry branch could freeze the whole desk — worse than the blank-stage defect it fixed) → per-session files, no lock, no cache, read→union→write; then the fork-recovery hook `inheritFrom` (a prompt-driven extension command can switch sessions outside the wrapper, so a stale pending inheritance seeded an unrelated session) → removed; inheritance lives only inside `lifecycleRpc` with the source confirmed before the RPC and the destination inside an enforced 1000 ms budget. Two late rounds on each lane were doc-accuracy only (astra: seven sentences the code did not honour; sol: a synonym), and both closed with a one-line LAND.

What the seat did itself: merges + conflict resolution (README contract-note sections ×3, punch-list), the last three doc/toast fixes on the integration branch, the corpus (`~/nana-pi/docs/reviews/desk-hardening-2026-09-09/`, briefs + 16 reviews + a round table). Frictions worth a ledger line: a worker overwrote the seat's `run-suite.sh` in the scratchpad root (same filename), so one "merged" suite run silently tested the wrong tree — scripts are now seat-named and workers told to stay in lane subdirs; the desk tests use fixed ports and cannot run across worktrees concurrently (three lanes produced spurious EADDRINUSE reds until run sequentially). Not done: the launchd desk was never restarted (Jake's call, still the NEXT); nothing pushed.

## 2026-09-09 — nana-pi: the tool-profile benchmark (built, reviewed ×7, run, decided) + lineup option (b)

Jake: "C but with proper measurements, comparing different tools on the same tasks, measuring token usage and runtime and result", then "the measurement tool can be persisted as part of nana-pi, and can be reused for future tooling", then "we're only using pi coding agent while pi actually has a long list of lineups" → inventory → pick (b) with dependencies listed in READMEs. An API outage killed the first build agents mid-way (nothing on disk); rebuilt from scratch.

The bench (`apps/bench/`) went through seven pre-spend review rounds. astra r1 FIX-FIRST (nested search spend uncounted; pi retries on by default; exit-0-on-error in json mode; undecidable rule) → r2 STOP → r3 NO-GO (a ReferenceError AFTER the paid probe; summary-model calls invisible because pi's Codex transport is WebSocket, not fetch; forgeable suite checker) → r4 NO-GO (sentinel forged from inside the allowed lines → design change to a nonce-signed trusted evaluator; probe ledger fail-open) → r5 NO-GO (nonce left on disk; two NEW regressions from a builder at 845k context → fresh worker + a mandatory stub-driven test of every orchestration path). Then the Codex backend content-filtered the evaluator SOURCE (twice, sanitized brief too) → Fable took r6 (NO-GO: the nonce as a string reaches a writable Buffer.from — a 3-line gap the 15-case matrix missed; interrupt drain race; executeRun never tested on a real fixture, which exposed workspace.diff taken against the pristine fixture) → r7 GO. Lesson for DOCTRINE: each reviewer lens caught a disjoint class; the builder's own adversarial matrix never caught its own blind spot.

Run: replicate 0 (44) checklist clean → reps 1-2 (88); 2.25M tokens, $6.07, 37 min, 0 retries/compactions/grader errors. Verdict applied mechanically: 132/132 correct is a ceiling effect, so every comparison is cost-only — B lean-code REGRESSION vs A (per-task 2.04×, +3.5%), B′ REGRESSION (the hint bought MORE exploration), C web-access INSUFFICIENT on research (3 runs with unmeasured nested spend, observed ≥5.6×, and `npm view` answered every research task) and REGRESSION on code (pi-web-access's tool schemas tax every turn, up to 5.55×). Default stays pi defaults; pi-web-access not installed; the research brief's "modest win" is contradicted for this workload. Lineup (b) landed: bench money on pi-ai `calculateCost`, desk sessions through pi's public parser + migration with a same-install invariant, Dependencies sections everywhere. Also: pi defaultModel → gpt-5.6-sol; hook status chips; nana code rename; Tools + hook-config settings (earlier in the day).

## 2026-09-08 (5) — nana-pi: Jake's live-usage batch (nana code rename, rail, tools + hook settings, hook visibility)

Jake's feedback after the hardening pass: default model → gpt-5.6-sol; collapsible left panel; rename "the pi desk" → "nana code"; hook and tool configuration under Settings; "make hook trigger visible? … I'm not seeing anything from my Windows session"; and a research ask on default tools. Fact-finding first (Explore agent over the desk; an Opus research agent over installed pi 0.84.4 dist/docs + ecosystem): the only always-on signal was the lifecycle chip — post-edit and gate were silent on the happy path by design, receipts never displayed, win32 notify swallowed every error; pi's default tools are read/bash/edit/write with grep/find/ls OFF and a system-prompt line steering to bash (Jake's 14-day session histograms confirm the seat never sees grep/find; only pi-subagents children do).

Two Opus 4.8 builds, each sol-reviewed (both LAND with SHOULDs folded): `5660ee3` nana-pack happy-path status chips for post-edit and gate + a bounded win32 notifier with in-app fallback and a shape-anchored PowerShell error-record classifier (localized-labels residual documented); `fcaa3a3` desk → "nana code": rail toggle persisted pre-paint, Tools tab + `-xt` per-spawn narrowing that respects a trusted project's replacing `defaultTools`, full nana-pack config form with user/project scope through the same destination guards. Research brief filed `994d37d`; the tool-profile pick (B lean-code / C + pi-web-access) is Jake's. Suites: pack 172, desk 352, all green. pi defaultModel switched (backup kept).

## 2026-09-08 (4) — nana-pi: the desk hardening pass (punch-list worked, nine commits)

Jake: "work on the punch list, use opus 4.8 as workers and utilize pi astra and sol for different levels of reviews." Stakes read: the desk is a loopback control plane (spawn/bash/config over pi RPC) — moderate blast, reversible via git, but the launchd desk keeps running whatever server.mjs it loaded, so nothing activates until Jake restarts it.

Shape: four Opus 4.8 workers on disjoint file sets (desk server · nana-stage · nana-pack extensions · docs/skills), each told to CONFIRM astra's leads against code before fixing and to ship failure-first tests through the real chokepoints (real server on ephemeral ports, real registered handlers, pi's own SessionManager as judge). Review ladder: gpt-5.6-sol per package → fold → commit; gpt-6-astra whole-unit over the four commits → fold → two more sol rounds on the desk folds. Every code package came back BLOCK on the first sol round and every finding was real (an unguarded JSON.stringify in the block validator; taskkill failures leaving run() pending; a symlinked `.pi` directory bypassing the leaf-only lstat; pi's `resolveToCwd` normalizing `~`/`@`/Unicode spaces where the receipt did not; a 50k-deep JSON event killing the desk through broadcast; a >1 MiB final session entry making the "fixed" rename sever context anyway; the EPIPE path bypassing teardown; a request-derived trusted root exempting its own symlink). astra's whole-unit pass added the resource-classification hole (a global-declared package living inside the project rode in via `-e` beside `-na`) and made the contract changes get declared in docs instead of commit messages.

Landed on nana-pi main: `f777638` docs/skills · `58645c5` nana-stage · `2efd435` nana-pack · `368f67f` + `53d4aab` + `e493043` + `ddffd6b` desk · `16da4c8` + `9209146`/`650bd54` docs/punch-list. Suites seat-run: nana-pack 104, blocks 112, desk 243 (+ stage e2e 11/30), all exit 0. Pre-existing and unrelated: `stage-chain*.e2e.mjs` fail at baseline `30aa4be` too — pi's user-level defaultModel is `gpt-5.5`, which the Codex provider dropped today; left for Jake.

What the pass did NOT do (honest, in `apps/desk/README.md` Known limits + the punch-list): buffers unbounded, client races, stage signing key per spawn (design B recommended), win32 taskkill branch untested, TOCTOU on every lstat guard documented not closed. Lesson worth one DOCTRINE line if it recurs: per-package review + whole-unit review caught DISJOINT classes (sol: local correctness; astra: cross-package policy + undeclared contract changes) — same shape as the three-layer finding in the construction-layer arc.

## 2026-09-08 (3) — nana-pi: evidence layer + adoption gap + a four-angle astra review

Roster (Jake): **gpt-6-astra** in Fable's slot for architecture + land reviews, **Opus 4.8** build workers, **pi-5.6-sol** the usual independent lens; the seat wrote briefs, verified every fix mechanically, integrated ([[feedback_seat_instructs_opus_works]]).

A strong external assessment (evidence-backed-workflow-before-harness, 8 PRs) came in — priced from the public README, no repo access. Grounding it against the real repo dissolved most of the 8 PRs: 5 already exist, §2A "advisory not governed" is already nana-pi's written posture, and PR2's gate-hardening re-acquires scope the sibling repo owns. The real contribution was the evidence layer. astra ruled the staging (evidence-first; defer the acceptance controller until dogfood shows a fresh-green-but-wrong-task miss) and hardened it.

Three lands on nana-pi main: **99f7438** content-bound check receipts in nana-post-edit — sha256 over the checker's INPUT CONTENTS (not filename), distinct status enum, deadline-aware monotonic timeout, best-effort; astra caught two blockers the seat's own read missed (timeout-killed-but-exit-0 → checks_passed; undefined ctx.signal → crash) plus a timeoutMs:0 regression, all verified empirically. **faaf3c5** a language-agnostic adopt-structure skill + a single-source "Working under nana-pi" AGENTS.md section shared into both scaffold roots (Jake's dogfood gap: adoption was language-only, the root AGENTS.md was nana-silent). **6803024** residuals from the four-angle review.

Answered from the code: hooks auto-activate from `pi install` at user scope (every session, no adoption); handoff/journal/lifecycle work in any existing project on defaults. Verified the pivotal fact: `loadProjectContextFiles` loads cwd + ANCESTORS only, never descendants — per-folder AGENTS.md apply only when a session runs in/below the folder; the doc now carries a read-on-demand baseline + a cwd-rooted-subagent option (subagents are an extension, not core pi).

Pre-close, Jake asked for a multi-angle review: four parallel astra reviewers (security, extension-correctness, desk, coherence). They caught real defects incl. residuals in this session's own lands (starter stub shadowed user gate/handoff config; nav over-promise; timeout feedback silent; a malformed-config crash class) — all fixed in 6803024 + a fractional-timeout follow-up. The larger find is pre-existing: the **desk** (localhost control plane) carries several SEV-high crash paths (URL-parse, stdin EPIPE, transcript-cycle infinite loop, rename-severs-resume) and security items (title-agent full tools + injection, handoff symlink in untrusted projects, stage XSS, DNS-rebind). Full triage in `~/nana-pi/docs/review-punchlist-2026-09-08.md`. Honest read: usable for personal happy-path dogfood, NOT hardened — gaps cluster in the desk. Jake's call: log the punch-list, don't fire the hardening pass autonomously.

Ops: sol's Codex backend stalled twice (astra carried review solo, reliably); the pi-review watchdog's flat-CPU stall detector is defeated by micro-CPU-creep (→ FRICTIONS). One seat error: a stray `&` on a run_in_background pi-review orphaned it (false "completed" notification) → relaunched as the backgrounded command directly.

## 2026-09-06 (2) — chat bubble LANDED (nana-pi 0a33a69) + perspective-puzzle game design LANDED at pi round 9 (nana-pi 5a50cde…); 1b deferred

Jake, after the 1b explanation: "we can come back and work on this slice later"; feature request —
chat as an e-commerce-style collapsible bubble; new slice — an interactive web game where chat
changes the game; then a design conversation that ran the whole afternoon.

**Bubble.** Drawer → fixed panel over the page behind a corner button, closed by default, badge on
`agent_settled` while closed, Esc closes, gate bar untouched. Two test failures on the first run,
both mine: the toasts container inherited `top/right` from the desk stylesheet and stretched across
the page (blocked the gate-bar click); the edge desk serves its OWN page HTML, which lacked the new
markup, so boot threw and readiness never reported. Fixed both; stage page e2e 30/30 (+6 bubble
checks), edge page e2e 10/10. edge-screener `97268b7` carries the markup.

**Game design.** Seat proposed toy-battle fog-of-war (rejected: "gotta be a new game"). Jake's
frame: human is the main player, agent unlocks the fun; escape-room / platformer where physics and
perspective change; his own worry — is the agent forced? Seat's test: forced if a menu could
replace it. Jake's reframe: a WORLD with free interaction that generates puzzles, not authored
puzzles. Systems list: gravity, BotW elements, light, 1D/2D/3D, material hardness. Seat: the
dimension flip forces a discrete voxel grid; TypeScript + Three.js for rendering only, no physics
library. Ratified in discussion: front-most projection, physics only in visible dimensions (top view
= no gravity), push-out-then-resolve re-embedding.

Then nine pi rounds on the design doc (gpt-5.6-sol, $0, ~4 min each). r1 BLOCK reshaped the plan:
the v1 agent surface was menu-replaceable (E1) and the kit had no state-sharing path (F1). Jake
ruled: v1 WITHOUT the agent; subtractions; nowhere-to-re-embed = loss; commit never a model tool;
F1 moot (pure headless sim carried). r2–r8 each closed one partial transition — fluids as cells,
three-step enterability + Sokoban pushing + step-up, vertical-intent passes, re-embedding RESERVED
(true world always consistent in v1), a v4 seat regression (dropped the true-destination solid
check) caught in r4, post-movement support snapshot, out-of-bounds = solid, `passableFromTop`
removed by subtraction, fragile freeze precedes buoyancy. r9: LAND, "an exact, terminating solver
over deterministic rules". Findings per round: 15→4→4→4→1→1→1→1→0.

**What converged it:** each brief demanded NEW counterexamples (not re-litigation) and stated a
verdict discipline (LAND on minors; BLOCK only for a partial/contradictory transition). Doctrine
line "iterated dual land review converges" bumped to [uses:6]; no new entry (ledger over cap, held
for the loops-vs-pi ruling).

Asset test: the design doc (L3 deposit, review corpus attached) + the kit (bubble). Mechanize scan:
the fold→brief→launch→verify-edit→commit cycle was hand-driven nine times; a
`design-review-round.sh` would save ~2 min/round — filed nowhere yet (one session's evidence; the
2+ rule applies). No nana-agent-loop code changed; map:check run at close.

## 2026-09-04 (4) — slice 2 BUILT + review-LANDED (4 pi rounds): the edge desk at :7321 (nana-pi f22d3c2…, edge-screener 686dd15…) — awaiting Jake's feel check

Jake: "let's continue the slice 2 work then" — the feel-check gate on slice 1 waived by
his go. Built the edge desk end to end the same night, per §11 of the frontend design.

**Step 0 changed the build twice.** `screen --focus` on the live vintage ran past 25 min
(killed) → chart series are an OFFLINE precompute (`scripts/desk_oos_series.py`, 108 s per
screen, weekly points; amihud done, 675 points, max dd −39.3%). pi-mcp-adapter 2.32.1:
runtime-registered servers are proxy-only → direct tools come from a PROJECT `.pi/mcp.json`
in edge-screener (`toolPrefix: none`, eager). The adapter bounds the WHOLE raw MCP result and
mcp 2.x echoes a dict return as pretty JSON text, so the 33 KB chart became ~80 KB and was
summarized even under a 64 KiB cap → tools return `CallToolResult` (one-line text +
structured blocks). Cap = block cap (65536), so the over-cap path fires only for blocks the
validator rejects anyway.

**Built.** Kit: `chart` block (≤4 series, ≤1000 points, monotonic x, finite/null y; the model
reads a per-series summary; inline-SVG render with hover + table view + legend; dataviz
palette light/dark); listener manifest keys `page` / `data` / `quick` + `agent:changed`
event on mutating tools. App: `edge_screener.desk` (reports parser with `path:line`
evidence, six block builders, tape/shelf/roster data CLI, `MCPServer` with six tools),
`desk/` page (tape · personas · reports shelf + stage), manifest → http://127.0.0.1:7321.
Deviations declared in design §11.6: NO Python validator (one validator; Python producers
test through node) and the host seam additions (design named "app API / store" and "its
page" but never provided them) — the reviewer rules on §5 in §11.7.

**Verified.** blocks 76 · listener 57 · origin 11 · stage page 24 · basketball chain PASS ·
**edge chain (real pi + adapter + Python MCP)** 19/19 exit 0 incl. a 512-byte-cap child
failing closed and `explore_screen` writing exactly one file · edge-screener full suite 691
passed, 92.9 % coverage (mypy: 81 pre-existing errors in tests/firm, untouched).
Screenshots light + dark inspected → two fixes (relative evidence refs; card values keep the
report's printed figures). Spend: ~10 real model turns (gpt-5.5 via pi) for the chain
tests + screenshots; $0 metered.

**Adversarial pi review (gpt-5.5 via Codex) r1 → REWORK, all MAJORs fixed (nana-pi
`0dd1590`, edge-screener `1ad8a11`; record in design §11.7):** cross-site GETs could
trigger the data commands → Sec-Fetch-Site + Origin gate; first-turn MCP readiness was a
6 s sleep → nana-stage reports `waiting/ready/missing` on the RPC status channel, the
listener holds the spawn on it and 409s early prompts, the e2e asserts the OBSERVED report;
chart validator admitted impossible dates and all-null series → real-calendar round-trip,
≥1 finite y, total-point cap 2400 fitting the byte cap; the panel's derived survivorship
column cited the wrong report → subtracted, every table row shows a visible `source` line;
no browser proof of agent:changed → new `stage-page-edge.e2e.mjs` (9 checks). Reviewer's
transferability ruling adopted and written into §1/§5: one canonical validator; the
page/data/quick + agent:changed seam is specified kit, "a design gap the design named but
failed to specify, not an app-bespoke failure". **Round 2 → REWORK: my two round-1 fixes
were themselves fail-open** (readiness defaulted to ready with no report; the data GET trusted
an absent Sec-Fetch-Site) → desk-owned fail-closed readiness (`waiting`/`ready`/`unreported`,
409 on prompts, silent-child test) and data refresh as a same-origin JSON POST (GET → 404);
page gates send and keeps a refused prompt (nana-pi `520fcf6`, edge-screener `e61b192`).
**Round 3 → REWORK** (one-shot readiness; conditional JSON guard; 200-with-waiting on a dying child) →
continuous tool watch with downgrade, unconditional JSON on the data route, 502 on exit
(nana-pi `1d1188d`). The real-chain test caught a regression the stubs cannot: pi AWAITS
extension handlers, so an always-on watcher awaited in `session_start` blocked the session —
detached it. Final: chain 21/21 · basketball 15/15 · listener 70 · browser 24 + 10 · blocks 80.
**Round 4 → LAND** (two MINOR residuals: watcher rejection boundary → fixed; 2 s downgrade
window → accepted). Lesson carried into the design doc: every security-shaped round-1 fix I
wrote was fail-open in the direction the reviewer had not looked yet (default ready, trusted
header absence, body-conditional guard) — a gate's default state IS the finding's answer. Ops lesson
(FRICTIONS-grade, one instance): the readiness heuristic keyed on the extension FILENAME
and hung the stub-pi listener test for 10 min (its no-op extension is named nana-stage.ts)
— key on the observed report, never on a name; and never run two e2e suites that share a
test port in parallel (EADDRINUSE 4421 masqueraded as a browser-test failure). Live edge
child from the screenshot run retired, manifest session reset, launchd desk restarted on
the fixed code so Jake's first session starts clean.

## 2026-09-04 (3) — slice 2 redirected to the EDGE DESK (edge-screener); loops-vs-pi assessment written for Jake's ruling

Jake, in one message: make the second UI-centric use case a trading dashboard from what
edge-screener has today (not AML), and asked whether governed loops are still worth doing
versus driving nana-pi directly. No build this session — design + assessment only; the
slice-1 feel check still gates the build.

**Edge desk (design §11 of `~/nana-pi/docs/agent-frontend-design-2026-09-04.md`).**
Grounding check on edge-screener first: ONE frozen tape (`live`, 504 names, daily
2010-01-04→2026-05-29, yfinance parquet, 77 MB), no live data, no positions/P&L/orders;
the `surv-run` vintage the standing survivorship verdict needs is ABSENT on this Mac
(`edge-screener verdict` fails here — committed reports are the only source for that
band); `explore --screen momentum --manifest live` = 2.8 s and is labelled NOT A VERDICT.
So "trading dashboard" over this repo is a RESEARCH desk, stated plainly in the doc. Page:
tape card + reports shelf + persona roster (app-owned), five tools returning blocks
(screen_panel, screen_detail with the first new block type `chart`, construction_table /
stop_table, direction_board, explore_screen = the one mutating tool, writes only under
`reports/explore/`). Tools as a Python MCP server via pi-mcp-adapter — this is what makes
slice 2 the transferability judge (details.mcpResult path, 16 KiB cap behaviour, the
kit's Python validator, mutation refresh, cell-level evidence). Minutes-long studies and
`pull` are scheduled-turn only (slice 1b's first real job = weekly tape pull). Step 0
before any build: time `screen --focus`; confirm the adapter's tool-registration mode.
§5 table gains the edge-desk row; AML deferred behind it; family-planner unchanged.

**Loops vs pi (`docs/loops-vs-pi-2026-09-04.md`).** Framing correction first: loops
already run ON pi, so the axis is governed-unattended-with-ceremony vs seat-attended
with an independent review lane. Evidence: run registry (since 08-09) holds 4 real
governed runs, last 2026-08-25 (toy-battle arena, error); September = 0 loops, 17
sessions, 34 nana-pi commits in 4 days, design+slice 1 in one day with 3 adversarial pi
rounds. Session-start lint today: doctrine 30, frictions 91, HANDOFF 670/400 — grooming
debt outrunning payoff. Top doctrine line [uses:29] (non-Anthropic pi reviewer) is
already outside the runner; amplification and lean-spec results say the round machinery
was never where quality came from. Recommendation: runner DORMANT not retired (like the
operator seat); attended work = seat + pi + independent pi review; carry worktree-land
discipline + acceptance-script-first into pi as skills only on demand; silence loop-ledger
lint while dormant; meter = registry on 2026-10-02. The one case still earning a loop:
unattended multi-round LLM work over a big corpus with a deterministic rail. NOT acted
on — Jake's ruling; HANDOFF carries it as an open ruling.

**Asset test:** grew the frontend design doc (§11) and a decision doc Jake reads; memory
`project_loops_vs_pi_ruling` + `project_ui_centric_frontend` updated. Spend: $0 metered
(seat only). Mechanize scan: the registry-by-month count was a hand-rolled python
one-liner — if the dormant ruling lands with a meter, `loop-status` should print real
runs per month (one line), not the seat.

## 2026-09-04 (2) — UI-centric agent frontend: design LAND (6 pi rounds) + slice 1 (fantasy basketball) BUILT and adversarially reviewed (nana-pi 8e8bc5e…d0d6ef2, basketball-geek 44e5e58…c6dcaa3)

Jake restarted the desk, then asked for a second frontend shape beside the
chat desk: the app owns the screen, chat drives it, one modular kit for
family-planner / AML investigator / fantasy basketball. Ruling in-session:
**chat is PRIMARY** (one channel; typed, clicked = UI-composed prompt, or
scheduled). Design doc `~/nana-pi/docs/agent-frontend-design-2026-09-04.md`
went through six pi (gpt-5.6-sol) rounds — REWORK ×5 then LAND — and got
SIMPLER each round: no `present` tool, no ref table, no generative UI. Tools
return code-authored blocks; a `nana-stage` extension validates + stamps them
into the session as a ledger (the ledger IS the stage) and replaces the model's
text with the canonical rendering; one desk listener PER APP (own origin,
manifest-only spawn, `-t` allowlist). A live desk exposure surfaced in round 3:
no Origin/Content-Type check → any website could POST /api/spawn.

Jake: "Let's build the fantasy basketball slice." Built in the doc's order,
tests before each next step: (0) Origin+JSON rule on the desk (11 tests);
(1) `desk-client.mjs` extracted, desk unchanged (E2E ×3 green before/after);
(2) block contract + reducer + hook logic (61 tests) + `nana-stage`;
(3) `apps.mjs` per-app listeners + manifests (43 tests vs a stub pi);
(4) basketball tools: `blocks.py` (board_table, player_card; accent-insensitive
exact match, partials refused with suggestions) + `blocks_cli.py` + the
`nana-basketball.ts` bridge (12 tests; repo gate 675 @ 94.4%);
(5) the stage host page (24 browser checks vs stub pi) + a REAL-chain E2E
(real pi child, real tools, 2 model turns: stamped block in the ledger,
canonical text, reducer equality, refused partial name → isError, ledger
unchanged). First screenshot caught 5 render defects (column-key collision
`#`/`$`, clipped row buttons, footer overlap, mixed decimals, badge dup) — fixed.

**Adversarial build review (pi gpt-5.6-sol), 3 rounds.** R1 REWORK, 3 blockers
the design review could not see: the stamp was forgeable (any co-resident
extension / hand-edited session file), an EMPTY `-t` allowlist silently meant
pi defaults incl. bash, concurrent spawns orphaned a child, a gate card vanished
before the server accepted the answer, the delta cursor never applied deltas,
the drawer replayed abandoned branches. Fixed: per-child `NANA_STAGE_KEY`,
HMAC-signed stamps, server drops unsigned/event-mismatched blocks live and on
the ledger read; empty allowlist refused; spawn serialized; gate waits for the
server; full active-branch replay. R2 REWORK: filtering forged entries SEVERED
the session-tree ancestry (blanked valid stages) → redact, never delete; no
replay on reconnect → every later desk_hello forces one; timed dialogs lingered
→ server-side expiry; sign the wire form; the key is process-wide → claim
narrowed + key scrubbed at load. R3: **LAND, no fixes** (recorded in the doc §10 with the
build-review corpus). Desk bug found on the way: pi reports tool failure in
`result.isError`; the desk drew ✓ on failures — fixed in desk + stage.

Live for Jake's feel check: **http://127.0.0.1:7320** (launchd desk restarted
twice, both times with zero live sessions — his two basketball sessions from
before the first restart are resumable from the desk). Feel verdict decides
slice 2 (AML: MCP-bridged tools, mutation refresh, cell-level evidence).

Spend: pi reviews on the Codex subscription ($0 metered); ~6 gpt-5.5 model
turns for the real-chain tests and screenshots. Asset test: the stub-pi +
per-app-listener harness is the reusable test pattern for every next app; the
block contract is the reusable artifact. Mechanize scan: the review-brief →
watchdog → closure-round loop was run by hand six + three times — a
`pi-review --rounds` wrapper that attaches the prior output automatically is
the obvious script (FRICTIONS).

## 2026-09-04 (1) — desk dogfood r2: stuck subagent widget cleared, live sessions get their names (nana-pi a265e87)

Jake's second dogfood report: a reviewer subagent box pinned above the editor
("partial · 14 turns · 36 tools" + its failed step) with no model/tokens shown,
and his basketball-geek session stuck at "(unnamed)" despite hours of use.

1. Stuck widget = upstream pi-subagents 0.64.0 bug, diagnosed to the line: the
cleanup predicate (async-job-tracker.ts terminalStatus) covers
complete/failed/paused/stopped but NOT `partial`/`rejected`. His reviewer run
died on a Codex usage limit → state `partial` → never scheduled for cleanup →
pinned in every future snapshot (the TUI has the same bug; the run sat live in
the desk_hello widget a day later — verified against the running server).
Desk now defends itself: terminal-state runs prune after a 60s linger at
render time, self-clear via timer (the stuck case sends no further events),
and finished runs show duration. Model/tokens can't be shown in the widget at
all — the RPC snapshot schema (AsyncStatusSnapshotNodeV1) simply doesn't carry
them even though the TUI renders both from in-process state; that parity gap
plus the terminalStatus hole are recorded as a dated addendum in nana-pi
research/pi-landscape for an upstream report. (The tool-card strip already
shows model+tokens for foreground runs since r1.)

2. "(unnamed)" forever = the title pipeline worked but wrote to the wrong
place for live sessions: the server derives a title and APPENDED session_info
to the file — a live pi child never re-reads its file, so get_state
sessionName (what the header and rail render) stayed unset. Verified live:
his session file carried "Adopt Python Stack Overlay" since 36s after start;
live state had nothing. Fix: applySessionName routes through the owning
child's set_session_name RPC (pi persists the entry itself AND updates state
— confirmed: pi appended its own linked session_info on the heal), broadcasts
desk_renamed (client refreshes header+rail), appends only for cold files;
/api/rename same routing. His live session was healed immediately via the
already-allowlisted RPC — no desk restart, no session kill.

Independent review (pi/Codex): 2 findings. Accepted narrowed — an
indeterminate owner scan (a child not answering get_state) no longer falls
back to a divergent file append on the derive path; it defers for retry.
Rejected — recursive child pruning: terminal steps under a RUNNING parent are
live chain history (TUI parity), and a terminal parent prunes its whole
subtree anyway. e2e: subagent-render extended (stale-partial pruned,
fresh-complete lingers w/ duration — 13 checks) + new browserless
title-live.e2e.mjs (live RPC routing, file untouched; cold append; rename
routing — 7 checks). All pass. Server fix activates on next desk restart
(launchd); app.js is live on browser reload. Lesson class: same as r1's —
state that a UI reads must be written where that UI reads it (live state via
RPC, not the file under it); and a viewer of someone else's lifecycle data
needs its own expiry clock for states the owner forgets to clean.

## 2026-09-03 (8) — desk dogfood report: subagent tracking readable, context meter survives compaction, handoff artifact protected (nana-pi 927b0b8 + 46d6413, pushed)

Jake's first real dogfood verdict ("actually quite usable now") came with three
report items; all three fixed in nana-pi.

1. Subagent tracking was numbers + JSON. Two surfaces, one cause: the desk
rendered the `subagent` tool generically (argSummary → raw JSON; streaming →
raw output dump), and pi-subagents' async widget arrives in RPC mode as a
`PI_SUBAGENT_ASYNC_JSON:{...}` machine snapshot the client is EXPECTED to
decode — the TUI draws its own tree; the desk printed the blob. Fix: a subtle
strip per child (agent · model · tools/tokens/duration + current activity +
task) built from the tool's `details` (results[]/progress[] per pi-subagents
shared/types.ts), head summary "agent — task", and a widget decoder. Raw
stream no longer rendered; args stay under the collapsible.

2. Context % stale after compaction. Upstream-by-design: pi's getContextUsage
returns percent:null after compaction until the next assistant reply (the TUI
footer shows "?"). The desk now stashes compaction_end's estimatedTokensAfter
and renders "~N% of X (est)" until a real percent returns. Verified: new
stub-pi e2e (apps/desk/test/subagent-render.e2e.mjs) — a fake `pi` binary
speaking the RPC JSONL protocol drives the REAL server + REAL app.js in
headless Chromium, zero model spend; 11 checks incl. mid-run strip, history
re-render parity, and the meter flip. PW_SHOT env makes it a visual harness.

3. "handoff.ts deleted by agent LOL" — it was .pi/handoff.md in
basketball-geek: untracked, AND both the file header ("edit or delete
freely") and the injected pickup prompt ("update or delete") INVITED the
deletion. A tidy agent obliged. Fix: nana-handoff now maintains
.pi/.gitignore with exactly `handoff.md` (append/dedupe, .pi/ stays
committable — nana-pack.json is tracked in adopters; default path only —
custom handoff.path git semantics belong to the owner), and all wording is
now "update in place; only humans delete". Deterministic test, 11 checks.
Jake's deleted handoff was RESTORED from the session file's compaction entry.

Independent review: pi/Codex found 1 MAJOR (pickup prompt hard-coded
.pi/handoff.md even when handoff.path is configured — pre-existing, fixed:
prompt now names the resolved path) + 1 MINOR (no .gitignore beside custom
paths — fixed); re-review VERDICT: LAND. Lesson class: a protocol payload
meant for client-side rendering (RPC widget snapshots, tool details) must be
decoded at the surface that shows it — dumping it IS the bug; and continuity
artifacts must be git-invisible AND prompt-protected, or an agent tidies them
away.

## 2026-09-03 (7) — desk unreachable: 4317 is OTLP and the machine filters it (nana-pi 2c99ce3, pushed)

Jake couldn't connect after the pool fix. Forensics: node LISTENing on
127.0.0.1:4317, event loop idle (sample: parked in kevent), yet EVERY client —
Chrome, sandboxed curl, unsandboxed curl — stuck in SYN_SENT. Same server on
port 4321: instant 200. So the machine filters loopback :4317 specifically —
it's the OpenTelemetry OTLP default, and Tailscale's network extension is
active (Docker vmnetd too). Filter not fought; port vacated: desk default now
7317 (DESK_PORT still overrides). Hosting moved OFF harness background tasks
to a launchd LaunchAgent (com.nana.pi-desk, KeepAlive, RunAtLoad) after the
harness twice proved unfit for a user-facing server (frozen SIGSTOP orphan;
inbound-filtered task). Verified 200 from sandboxed + unsandboxed clients.
Lessons: never default a local tool to a well-known telemetry port; long-lived
user-facing servers get real service management, not agent-tool lifetimes.

## 2026-09-03 (6) — desk semi-freeze fixed: browser connection-pool exhaustion (nana-pi 7af98de, pushed)

Jake's bug report: after Browse + close, the rail wouldn't collapse/expand and
session clicks didn't load — alive but wedged. Root cause: Chrome's ~6-per-host
connection budget vs the desk's held connections — pick-dir POST open for the OS
dialog's whole lifetime, title-derivation holding one request per derivation for
minutes, SSE, and the 15s rail burst. Once exhausted, every fetch queued forever
(no client timeouts) and each rail tick grew the backlog — a self-sustaining
wedge. Fix: pick-dir → start-then-poll contract ({id} + short GET polls,
single-flight freed on delivery); title derivation → server-side serial queue
(one batched enqueue call, names surface via normal refresh); rail refresh
overlap-guarded with AbortSignal.timeout so stalls can't compound. Verified:
poll cycle incl. cancel + guard-free, batch derivation lands, settings E2E
11/11, browser regression PASS. Desk on :4317 restarted with the fix (no live
sessions at restart). Lesson class: LONG-LIVED work must never ride a held
browser request — start-then-poll or server-side queues.

## 2026-09-03 (5) — examples census ruled, nana-handoff built, stack comparison, TRUST HOLE closed (nana-pi a07ab2e, pushed)

Jake ruled the census shortlist: plan-mode/preset REJECTED as extensions (plan
belongs as a skill command, dev-plan-style), git-checkpoint ENABLED (installed to
~/.pi/agent/extensions, load-verified), handoff redesigned per his spec: compaction
should AUTO-write a handoff and session start should AUTO-pick it up — the loop
repo's HANDOFF.md pattern. Built nana-handoff in nana-pack: session_compact →
.pi/handoff.md (last wins), startup/new sessions inject it (resume/fork skip —
context already present), journal events both directions. E2E: padded RPC session
(compact refuses tiny sessions — floor is keepRecentTokens vs entry tokens) →
compact → file with real structured summary → fresh session's model confirms the
section in its system prompt. Probe trap: mcp adapter live-fired mid-test (model
stored a memory through the bridge).

BIG review catch (pi/Codex FIX-FIRST): project .pi/nana-pack.json was honored
REGARDLESS of pi project trust — a hostile repo could relax the gate via
allowPatterns, define post-edit COMMANDS (arbitrary exec on first edit), or point
handoff.path at ~/.ssh/* for prompt-exfil. Pre-existing hole, surfaced by the new
config key. Fix: loadConfig(ctx) merges project config only when
ctx.isProjectTrusted() (pi 0.84.4 API, documented for exactly this), fail-closed
when absent. Deterministic 5-property test persisted at
packages/nana-pack/tests/config-trust.test.mjs.

Also delivered docs/nana-stack-comparison-2026-09-03.md (dev-kit vs loop vs pi
matrix): continuity gap closed by nana-handoff; remaining real gaps = dogfood
(still the gate), memory usage discipline (bridge live, habits absent — ~10-line
global AGENTS.md candidate), lean plan/debrief skills, ts-review. Non-gaps to
resist: loops/teeth/eval/wikis in pi-land. pi stack's unique edge: native win32 +
update-relationship scaffolds.

## 2026-09-03 (4) — desk settings window + MCP bridge + derived titles (nana-pi 86e8527, pushed)

Jake's build list: MCP bridge, derived titles for unnamed historical sessions,
and a full settings window. MCP: research (2 days old) predated the ecosystem —
web-vetted the field and adopted `pi-mcp-adapter` (community standard: 1.4k★,
761k dl/mo, single ~200-token proxy tool, lazy connects — the context-cost
philosophy that kept MCP out of pi core, solved) + `pi-subagents` (same author;
pi has NO native subagents by design). Verified the whole bridge chain live:
pi → proxy tool → spawned the real Python memory server over stdio → all 11
memory tools listed. Titles: /api/derive-title runs isolated headless `pi -p`
(codex $0) on the first user message, persists a session_info entry — once per
session ever; client backgrounds it for unnamed sessions in expanded groups
(serial, 12/load). Real catch: pi -p under execFile hangs to timeout because
piped stdin never closes — stdin.end() fixed it (manual shell runs masked it).
Settings modal: 7 tabs (Skills/MCP/Models/Context/Agents/Nana pack/Session)
over settings.json (whitelisted-key patch, never whole-file), mcp.json,
nana-pack.json, ~/.pi/agent/AGENTS.md + project context files, pi-subagents
agent files; every write backs up .bak. pi review FIX-FIRST B+D → delete now
.md-only/realpath-confined/rename-to-.bak, tab panes detached per invocation,
folder-list edits re-read state. E2E 11/11 + browser regression green.

## 2026-09-03 (2) — desk session-management rework from Jake's feedback (nana-pi 3f16e11, pushed)

Sibling-repo session continuation; no loop-program changes. Jake's feedback list:
collapsible per-workspace session groups (most-recent open by default, prefs
persist), inferred+editable titles (live = set_session_name RPC; historical =
POST /api/rename appending the exact session_info shape pi persists — verified
against 304 real entries; empty clears to inferred), the wire REMOVED (his
verdict: confusing/noise — panel, endpoint, styles gone; journal file stays),
spawn selector → repo browser (/api/browse, git repos marked), and per-spawn
skill/extension toggles (/api/resources enumerates pi's documented locations:
global/project dirs incl. ancestor .agents/skills walk, plain-path settings
entries, npm/git/local packages). Key facts grounded in installed pi 0.84.4
docs: pi has NO MCP (extensions are the toggle surface — told Jake straight);
`--skill`/`-e` stay additive under `--no-skills`/`--no-extensions`, so all-on
spawns flagless (pure pi defaults) and any narrowing passes exactly the checked
set. Also answered his "no bash for windows" error from pi source: Git Bash at
%ProgramFiles% → PATH → shellPath in settings; README got the actionable fix
(51488ef).

Review: pi/Codex two rounds. R1 FIX-FIRST A–E — real catches: trust-bypass
(unchecked trust + checked project extension still loaded project code via
explicit -e → project items now server-tagged project:true and locked off
untrusted), empty-name rename couldn't clear (readers now take any string,
"" → inferred), npm:-prefixed package sources unresolved, missing ancestor
.agents/skills walk + nested loose-md rule, browse-response race (seq token),
README overclaims. R2: all PASS, VERDICT LAND. E2E on-box: rename round-trip,
narrowed spawn shows exactly skill:spec in get_commands vs 8 default, synthetic
ancestor/project-flag probe, browser double-msg regression (playwright borrowed
via repo-root node_modules symlink — NODE_PATH doesn't work for ESM).

## 2026-09-03 (3) — desk: native OS folder picker (nana-pi 32c4140, pushed)

Jake's follow-up on the repo browser: he meant the REAL Finder/Explorer dialog,
not an in-page tree. Since the desk binds 127.0.0.1, server display == browser
display — POST /api/pick-dir opens the native dialog server-side (osascript
choose-folder in a Finder tell block; powershell -STA FolderBrowserDialog with
TopMost owner; zenity) and returns the absolute path web pages can't get from
their own pickers. Fixed-string scripts, single-flight 409 guard. In-page
/api/browse subtracted. Also fixed popover()'s {once:true} outside-click bug
(first inside click disarmed closing). Mechanically verified on darwin incl. a
live dialog open/busy/cancel/reopen cycle; pi review FIX-FIRST → directory
check added to /api/resources + /api/spawn (typed file path used to validate).

## 2026-09-03 — nana-pi win32 compat pass: desk serves + spawns on Windows, cmd-safe paths, gate parity (nana-pi 39578f9, pushed)

Sibling-repo session; no loop-program changes (HANDOFF untouched, no loops run).
Jake hit Windows path issues on the nana-pi install; full compat sweep found five
real breaks. (1) Desk couldn't serve its own UI on Windows — `new URL(import.meta.url).pathname`
yields `/C:/…`; → `fileURLToPath`. (2) Desk couldn't spawn pi — npm installs a `.cmd`
shim spawn() can't launch shell-less; → win32 shell spawn with env-var arg indirection
(`"%NANA_PI_ARG_n%"` on the line, value in child env — cmd expands once, non-recursively,
so spaces/&/%/quotes in values are inert), plus `taskkill /t` tree-kill so killing a
session doesn't orphan pi behind cmd.exe. (3) nana-post-edit's sh-style `quote()` doubled
every `\` in `C:\` paths through cmd.exe; → same env-var mechanism (`%NANA_PI_FILE%`).
(4) Skills' copier commands used bash `\` continuations that break PowerShell; → one line.
(5) Gate had no cmd.exe destructive parity; → `rd|rmdir /s`, `del|erase /f/s/q`,
`format <drive>:` with a deterministic pos/neg regex suite (`ruff format c:\…` and
`del build/sub` proven non-matching). Cosmetics: client collapses `C:\Users\<x>` to `~`,
basenames split on both seps; README gains Git-for-Windows prereq (pi's bash tool = Git
Bash per pi docs/windows.md) + PS5.1-safe install; v0.4.1, deliberately NO v* tag
(templates unchanged — a tag would red every generated project's drift CI for nothing).

Review: pi/Codex, two rounds per the authorside policy. R1 FIX-FIRST — cmd metachar
injection via unquoted args (already fixed pre-verdict by quote-everything), `%VAR%`
expansion inside cmd quotes corrupting legal `%` paths (→ the env-var indirection),
missing `erase` alias. R2 passed post-edit + gate, one residual on spawn (embedded `"` /
trailing `\` breaking the quote at argv parsing) → values sanitized at the boundary
per prescription. Verified on this box: syntax checks, gate regex suite green, desk
boots + serves on darwin. Honest cap: the win32 branches are reasoning+review-verified
only — nothing here executed on a real Windows box; Jake's smoke test is the close.

Sibling-repo session; no loop-program changes (HANDOFF untouched, no loops run).
Jake asked: is nana-pi self-contained — full experience from the repo alone? Audit
found one real break: both READMEs claimed `pi install git:github.com/j-wanger/nana-pi`
works, but pi reads the pi manifest at the CLONE ROOT and the pack's package.json
lives in packages/nana-pack/ — a git install discovered nothing. Everything had run
on Jake's local-path install, so the claim was never exercised (the pack README even
said "subdir support via manifest" for a manifest that didn't exist).

Fix: root package.json mapping the subdir in (`pi.extensions/skills` →
packages/nana-pack/*), pi core declared as an OPTIONAL peer dep so npm never
auto-installs it into consumer clones, plus a one-stop Install section in the README
(prereqs + pack + copier + desk). Verified end-to-end from GitHub after push:
`pi install -l git:` in a throwaway dir, then RPC get_commands under an EMPTY fake
HOME (kills the loaded-from-global ambiguity) — all 8 skills discovered AND the
lifecycle journal appeared under the fake home, proving extensions execute from the
git clone; desk served /api/sessions straight out of the fresh clone. No new tag cut:
pack-only change, and a v* tag with zero template delta would red the template-drift
job on every generated project for nothing. README notes tags ≤ v0.4.0 predate the
manifest, so pinned installs need a commit ref until the next real tag.

Follow-up same session, Jake's call: the flagged uncommitted desk dark-theme work
(THEME_ORDER cycling + pre-paint theme script + dark token block) COMMITTED as
nana-pi b5f4f39 after a browser pass (cached chromium via NODE_PATH; networkidle
never fires on the desk — SSE holds connections open, wait on the element): cycle
auto→light→dark, dark tokens paint, persists across reload, zero console errors.
Desk README's stale "one :root token block" line fixed with it.

## 2026-09-02 — nana-pi shippability: 324-agent research → four picks ratified → first wave landed (nana-pi fc1176f)

Jake opened with two things: the desk double-messages every sent prompt, and a
research mandate — coding-agent best practices (Python + TypeScript), whether to
fold nana-dev-kit into nana-pi, and how to make nana-pi generate modular,
well-structured code out of the box (file-size constraints, per-folder .md or a
map). Opus-4.8 subagents throughout, per instruction.

Research phase: two repo inventories (nana-pi map incl. pi 0.84.4 extension
mechanisms; full dev-kit portability split) + three deep-research workflows (324
agents, ~76 sources, 3-vote adversarial verification) distilled into
`nana-pi/research/coding-agent-best-practices-2026-09-02.md` with a
refuted-claims ledger — notably the strong empirical case for hard line caps
FAILED verification (rho=0.94 refuted 0-3), agentic bloat is architectural (God
Class) more than long-method, context files measurably lose adherence past
~150-200 instructions, and llms.txt is published everywhere but consumed
~nowhere. Options doc with four picks → Jake ratified all four.

Landed (nana-pi f2461a9..fc1176f): (1) desk fix — the E2E harness (real
Chromium + real pi child) first REPRODUCED the bug and exposed an
echo-beats-fetch race the static diagnosis missed; fix = append-before-POST +
content-matched swap of the echoed user message_end; regression test checked in.
(2) copier templates py+ts (uv/ruff/mypy-strict/pytest src-layout ·
pnpm/strict-tsconfig/Biome/Vitest), both generating green projects end-to-end,
with `.copier-answers.yml` drift management. (3) postEdit presets: format+lint
per edit, 500/300-line caps (verified both directions), typecheck. (4) lean
nested AGENTS.md, folder-by-feature. (5) nana-pack v0.2.0: six skills all
loading in pi (`skill:scaffold-py/ts, py-lint/test/review, spec`). Potholes
absorbed into templates: pnpm 11 moved build-script allowlisting to
`pnpm-workspace.yaml` `allowBuilds`; Biome tab-style configs; nana-post-edit's
config key is `run` not `command` (caught by reading the chokepoint source).

Independent pi review (gpt-5.5, watchdog wrapper): FIX-FIRST, 2 findings — the
multi-client FIFO-swap hazard (real; fixed as content-match, E2E re-passed) and
a package_name-derivation MAJOR (refuted with evidence: the replace filter
exists and the smoke derived demo_py). Closer: Jake asked for the desk in the
loop-desk design language — reskinned (66e5b8a) to the Orchestr paper-studio
tokens (same selectors, one :root block; mono-structure/sans-prose, semantic
go/warn/stop, terracotta accent), E2E re-passed under the new skin, screenshot
delivered. Next: dogfood a generated project, then decide Tier-2 (test-ran
gate) by feel. Wikis + memory-server stay deferred (pi core has no MCP).

## 2026-09-01 (2) — the pi desk: TUI-parity rework landed (nana-pi 7db8c87) on Jake's "usable but rough" verdict

Jake's feel verdict on the desk pilot arrived from outside: usable, rough, the
official TUI way more complete — "the aim should be at least replicating the
main capabilities of the TUI in the UI." Ground truth came from the installed
pi 0.84.4 package docs (usage.md = TUI surface, rpc.md = what a client can
drive), not memory; the bar is now an auto-memory
(`feedback_pi_desk_tui_parity_bar`).

The biggest hole was functional, not cosmetic: the pilot never handled
`extension_ui_request`, so a nana-gate escalation in a desk-driven session
would block forever with no human able to answer. The rework covers the whole
extension-UI protocol (dialogs→modals answered over stdin, notify→toasts,
setStatus/setWidget/set_editor_text), plus model/thinking pickers, session
rename/fork/clone/new/compact/export, stats footer with context meter,
markdown transcripts with tool cards (full args, streamed output, edit diffs),
`!` bash with live deltas, queue bar with TUI Esc reclaim semantics, `/` and
`@` completion, image paste, and branch-aware historical views. Architecture
shifted from event-buffer replay to desk_hello snapshot + get_messages render
+ message_end-authoritative deltas + settled resync.

Smoked end-to-end at curl level: real model turn (gpt-5.4-mini, $0.0009),
dialog round-trip via a throwaway `-e` extension, and a REAL gate escalation
(rm -rf → dialog on the SSE stream → Block answered via API → "nana-gate:
blocked by user"). Independent pi review (gpt-5.5 via the watchdog wrapper):
FIX-FIRST, 3 real findings — Esc-on-completion bubbled into reclaim+abort of
a live turn, wire rendering interpolated journal fields into innerHTML (XSS in
an origin that can spawn processes), export temp file leaked on read failure.
All fixed, re-checked, committed (nana-pi `7db8c87`); :4317 restarted on the
new code. Catch to own honestly: restarting the old desk server killed Jake's
idle basketball-geek child from his trial — session file persisted, resumable,
but the live check should have preceded the kill, not shared a pipeline with it.

Open: Jake's second feel pass (the parity claim is now his to judge); `@file`
completion inserts a path, not TUI-style content attachment; /tree branch
jumping has no RPC command (fork/clone are the desk's branch tools); Windows
box still pending from session 1.

Jake opened a new program: pi as a primary coding-agent platform + an
interactive UI surface, both OSes, Codex sub + local models, all four hook
classes (gate / post-edit / lifecycle / notify) as hard requirements. Work
lives in the new sibling repo `~/nana-pi` (this repo untouched; pointer =
auto-memory `project_pi_platform_adoption`).

Research pass 1 (104-agent deep-research, 24/25 claims 3-0): ADOPT, no
dealbreakers — but the coordinates moved (2026-05-07: `earendil-works/pi`,
npm `@earendil-works/*`; @mariozechner deprecated). Compaction, sessions,
providers (incl. openai-codex + llama.cpp first-class) all native; hooks =
extensions, verified event-by-event against docs (tool_call blocks
fail-safe, tool_result modifies, full session lifecycle, ui.notify/events
bus). No first-party GUI exists — TUI/RPC/SDK are the surfaces. Persisted:
`nana-pi/research/pi-landscape-2026-09-01.md` + raw artifacts (1b52a6b).

Build: pi 0.80.9→0.84.4 (Node floor 22.19 cleared); nana-pi-pack v0.1.0 —
gate (live-verified: headless rm -rf BLOCKED fail-closed), post-edit
(marker fired on real write), lifecycle (JSONL journal at
~/.pi/agent/nana-journal.jsonl), notify (UI-safe) — installed user-level via
`pi install`, so the gate now rides EVERY pi run incl. the pi-review lane
(e117100). Local models: existing llama.cpp `local` provider documented,
no ollama/LM Studio present (port-1234 false positive re-checked).

Research pass 2 — Jake's standing rule captured: deep-research subagents on
OPUS 4.8, not Fable (script variant with model pinned at all 5 call sites).
105 agents, 23/25: UI shortlist = pi-agent-dashboard (only proven
native-Windows, stale-scope flag) / SDK+ws web UIs (current scope,
single-maintainer) / Zed-only ACP lane; synthesis + seat agree: build-thin-
on-SDK beats adopting for load-bearing. The census the workflow missed got
closed seat-direct: 5,523 gallery packages, 8,868 npm, awesome-pi; adopt
candidates pi-web-access/pi-subagents/pi-lens/pi-plan-mode/pi-mcp-adapter
(source review first — full system access). Overlap flagged: community
permission-system/pi-lens vs our gate/post-edit — compare, never run both
per hook class (ffd8eef).

Jake: "pilot our own pi-dashboard" → **the pi desk** built (78ebfa5,
apps/desk/): zero-dep Node server + vanilla JS; RPC-subprocess attachment
(deliberate over in-process SDK — nana-pack rides along, proven in smoke);
sessions rail (24 workspaces read from JSONL trees), live drive
(prompt/steer/follow_up/abort/kill, max 4 children, LF-only framing per
upstream readline warning), and the signature "wire" — live tape of ALL pi
session lifecycle on the machine, fed by our own journal. Smoked
end-to-end: DESK OK deltas streamed, traversal guard held, zero orphans on
shutdown. Jake off to feel it: `node apps/desk/server.mjs` → :4317.

Open: Jake's feel verdict on TUI + desk; Windows box (Node ≥22.19, Git for
Windows, pi install); gate-vs-@gotgenes/pi-permission-system comparison;
steer-mid-turn + branched-session rendering untested in the desk.
