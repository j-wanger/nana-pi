# Shared memory (user · feedback · reference)

Lives at ~/.claude/nana-memory/shared, symlinked as `shared/` inside every project's auto-memory dir. Rules on how to work, who the owner is, and external references go HERE; project-level facts go in the project's own memory dir. Printed at every session start by the global hook.

