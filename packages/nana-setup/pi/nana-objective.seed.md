# Objective and current priority

*Single source of truth. Printed at every session start (Claude Code: the global SessionStart
hook; pi: the nana-pack objective extension). Every session must be able to say which of the
two lines below its spend served. Point `objective.path` in `~/.pi/agent/nana-pack.json` at a
real repo's `OBJECTIVE.md` once you have one — this file is only the starting placeholder.*

**Objective (since YYYY-MM-DD):** <the one thing this machine's work is for>.

**Current priority (since YYYY-MM-DD):** <the one lane open right now>.
