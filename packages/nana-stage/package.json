{
	"name": "nana-stage",
	"version": "0.1.0",
	"description": "nana-stage: the stage ledger for UI-centric agent frontends — validates blocks returned by tools at tool_result, stamps provenance, appends nana-block session entries, and replaces tool text with the canonical block rendering",
	"keywords": ["pi-package"],
	"license": "MIT",
	"type": "module",
	"pi": {
		"extensions": ["./extensions"]
	},
	"peerDependencies": {
		"@earendil-works/pi-coding-agent": "*"
	},
	"peerDependenciesMeta": {
		"@earendil-works/pi-coding-agent": {
			"optional": true
		}
	}
}
